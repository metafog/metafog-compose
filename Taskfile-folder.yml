vars:

tasks:
  default:
    cmds: 
      - loop:
        folder: /Users/rk/MYDISK/Work/temp/out
        run: zip
        parallel: 4
  
  zip:
    cmds:
      - echo "Zip File - {{.ARG}}"
      - echo "Zip File {{.ARG}} done."
